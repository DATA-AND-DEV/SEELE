# SDD ledger — plan: docs/superpowers/plans/2026-09-06-caminho-entre-pares.md

Spec: docs/superpowers/specs/2026-09-05-caminho-entre-pares-design.md (lida)
Branch: malha/caminho-entre-pares, a partir de f7512ef na main
Worktree: scratchpad/wt2 — o diretório principal está em desenho/mods com outra sessão

## Scan de pré-voo

### Pares de tarefas que dividem arquivo ou interface

| par | produz → consome | achado |
|---|---|---|
| T1→T2 | `Identidade`, `ErroDePar` → `passar_a_atender` | ok |
| T1→T3 | `ErroDePar::ImpressaoNaoBate` → verificador | ok |
| T3→T4 | `config_de_cliente` → `ligar` | ok |
| T4→T7 | `ParLigado` → `PorOndeAssistir::Par(Box<..>)` | corrigido antes do commit do plano (era sem Box) |
| T4→T8 | `ParLigado` → `repassar` | ok |
| T4→T9 | `ComoChegou` → asserção de integração | ok |
| T5→T6 | `EmprestarSubida`, `SirvaTelaPara` → despacho | ok, T5 vem antes |
| T5→T7 | `AssistaTelaPor`, `ParFalhou` → braço do enlace | ok, T5 vem antes |
| T1,2,3,4,7,8 | todas escrevem `par.rs` | aditivas e sequenciais; nenhuma reescreve a outra |
| T6→T9 | `Pares::escolher` → cenário do teste | ok |
| T7,T8→T9 | `por_onde`, `repassar` → cenário | ok |

### Cada tarefa contra si mesma

| tarefa | achado |
|---|---|
| T1 | **API do `rcgen` errada** e **`rcgen` é dev-dependency**, não dependency. Ver R1 e R2. |
| T2 | o guarda do Step 5 não prende — o próprio plano diz isso e aponta a T4 como a prova real. Aceito. |
| T3 | `ConfereImpressao.provedor` pode ficar sem uso. Ver R4. |
| T4 | `classificar` compara texto de erro; o plano manda tentar o caminho limpo antes e explicar se não der. Aceito. |
| T5 | ok — `postcard` é dependência do proto |
| T6 | `emprestando: false` não diz como chega em `declarou`. Ver R3. |
| T7 | ok |
| T8 | `abertura_clone` aparece no teste sem ser definido; a nota do plano avisa. Aceito. |
| T9 | dois `todo!()` deliberados, com contrato escrito. Aceito. |
| T10 | documento, sem código |

### Rulings antes de começar

- Ruling R1: `rcgen` **move** de `[dev-dependencies]` para `[dependencies]` no seele-core, versão 0.14.8 intacta — declarar duas vezes deixaria as duas divergirem. Custo se errado: nenhum, é fato conferido no Cargo.toml.
- Ruling R2: a construção do certificado na T1 passa a ser a forma provada em `crates/seele-core/src/tela.rs:2049` (`cert.der().to_vec()` e `PrivatePkcs8KeyDer`), e não a que eu tinha escrito. Custo se errado: erro de compilação, pego na hora.
- Ruling R3: `EmprestarSubida { emprestando: false, .. }` chega em `Pares::declarou` com `impressao` **vazia** — é o que o `if impressao.is_empty()` já trata como «deixei de emprestar». O implementador da T6 faz essa tradução no despacho do `session.rs`. Custo se errado: alguém que desligou o empréstimo continuaria sendo escolhido.
- Ruling R4: se `ConfereImpressao.provedor` não for usado pelo `impl` do `rustls`, o campo sai. Um campo morto para calar o compilador é o começo de um campo morto que ninguém entende. Custo se errado: nenhum.

## Progresso
Task 1: implementador DONE (commit c78b671; 2 testes novos, 253 no crate, fmt e clippy limpos; guarda provado por reversão). Revisão despachada sobre 3aa13aa..c78b671.
Task 1: revisão limpa — spec ✅, qualidade aprovada, 0 Critical, 0 Important.
Task 1: minor (deferred): `impressao` tem braço de cadeia vazia inalcançável e sem teste; um comentário dizendo por que ele existe bastaria.
Task 1: minor (deferred): o doc do módulo cita «§5.1» e «a spec de 05/09» sem caminho de arquivo — convenção que outros módulos já seguem, não é regressão desta tarefa.
Task 1: ⚠️ do revisor resolvido por mim — `ErroDePar::{Escuta,NaoAlcancou,ImpressaoNaoBate}` estarem sem chamador é esperado: a T1 entrega só a primitiva, e a T4 é quem as consome. Não é lacuna.
Task 1: complete (commits 3aa13aa..c78b671, review clean)
Task 2: implementador DONE (commit a3deb54; 3 testes em par::, 254 no crate, clippy limpo). Revisão despachada sobre c78b671..a3deb54.
Task 2: revisão — spec ✅, qualidade aprovada com 1 Important (plan-mandated) e 1 Minor.
Task 2: Ruling: o bloco `spawn`/`sleep`/`abort` do teste sai inteiro (era verbatim do meu plano, Step 1). Ele não afirma nada — o `JoinHandle` nunca é lido, então nem um pânico chegaria ao teste — e um bloco que parece conferência sem conferir é o «cenário se passando por instrumento» que o glossário registra ter sido tirado da interface deste produto. A asserção de porta mais a nota sobre a Task 4 são a verdade inteira do que este teste prova. Custo se errado: perde-se um `accept()` sendo exercitado uma vez, que não afirmava nada de qualquer jeito.
Task 2: Ruling: a frase «E ela de fato aceita» sai junto — ela alega o que a frase seguinte desmente, e some com o bloco. Custo se errado: nenhum.
Task 2: minor (deferred): nenhum além do acima; o Minor do revisor é resolvido pelo mesmo conserto.
Task 2: fix round 1/5 despachado ao implementador original.
Task 2: fix round 1/5 (2 addressed, 0 open — bloco vazio removido, frase contraditória removida; commits a3deb54..3938861). Sem quebra nova.
Task 2: complete (commits c78b671..3938861, review clean)
Task 3: implementador DONE (commit 861b3b4; 4 testes em par::, 255 no crate, clippy limpo; guarda provado por reversão).
Task 3: correção de despacho meu — o `TofuVerifier` mora em `crates/seele-core/src/tofu.rs`, não em `client.rs` como eu disse. O implementador conferiu e copiou da fonte certa. Despachos futuros devem dizer `tofu.rs`.
Task 3: concern do implementador: dois `#[allow(dead_code)]` em `ConfereImpressao` e `config_de_cliente`, porque só a T4 os chama. Levado à revisão como ponto de julgamento.
Task 3: revisão — spec ✅, qualidade aprovada, 0 Critical, 0 Important. Verificado além do diff: os três métodos delegados batem exatamente com o `TofuVerifier` de `tofu.rs`; nenhuma divergência.
Task 3: Ruling: os dois `#[allow(dead_code)]` ficam na T3 e **saem na T4**, quando `ligar` passar a chamá-los. O revisor tem razão que o precedente citado (`subida_no_arranque.rs:140`) não é análogo — lá o campo é permanentemente não lido por desenho, aqui é andaime temporário entre tarefas do mesmo plano. Andaime é aceitável enquanto é andaime; vira defeito se sobreviver ao consumidor. Levado ao despacho da T4. Custo se errado: dois `allow` órfãos que ninguém entende depois.
Task 3: minor (deferred): `ConfereImpressao::nova` sem doc e sem `#[must_use]`, ao contrário de `TofuVerifier::new`. Veio verbatim do meu plano. A T4 encosta nesse código — corrigir lá.
Task 3: complete (commits 3938861..861b3b4, review clean)
Task 4: implementador DONE (commit a6eee0d; 6 testes em par::, 257 no crate, clippy limpo; dois guardas provados por reversão separada). Revisão despachada sobre 861b3b4..a6eee0d.
Task 4: Ruling (defeito de desenho meu, achado por medição): instalar o `ServerConfig` não faz ninguém atender — o quinn enfileira o `Incoming` e cala até alguém chamar `Endpoint::accept()`. O plano não tinha tarefa nenhuma para isso. Inserida a **Task 5**, e as antigas 5–10 renumeradas para 6–11. Custo se errado: a Task 10 (costura) travaria sem ninguém saber por quê.
Task 4: Ruling (defeito de desenho meu, pior que o primeiro): `with_no_client_auth()` significa que quem **aceita** não confere ninguém. Como os dois lados discam, metade das ligações ficaria sem conferência, e quem empresta serviria quadro a qualquer um que alcançasse a porta — contra o que a spec promete por escrito. A Task 5 instala um verificador de certificado de cliente contra a `impressao` que `SirvaTelaPara` já carregava sem uso. Spec emendada com o §3.2.1. Custo se errado: um buraco de segurança no caminho novo, e a promessa da spec seria falsa.
Task 4: Ruling: o afastamento do implementador em relação ao brief (Arc<Mutex> no lugar da comparação de texto; três asserções a mais no segundo teste) é levado à revisão como ponto de julgamento, não pré-julgado por mim.
Task 4: deferred → Task 11: `ComoChegou` classifica por faixa de IP, então hairpin de NAT contaria como `Furo` sem ter havido furo — o roteiro de duas máquinas tem de registrar a topologia junto do número, ou a métrica infla.
Task 4: revisão — spec ✅, qualidade **needs fixes**: 2 Important, 4 Minor. Os três afastamentos do implementador foram julgados corretos e justificados (vaga por discagem correta sob concorrência, asserções extras necessárias para o guarda morder, auxiliar de atender confinado aos testes).
Task 4: Ruling (achado Important 2, plan-mandated): a preempção sai. Eu justifiquei no brief **precedência no fim** («ImpressaoNaoBate ganha do silêncio») e escrevi **preempção** — um único endereço respondendo com certificado errado mata a discagem inteira, mesmo que o endereço legítimo fechasse um instante depois. Com a lista misturando LAN e público, um endereço obsoleto ou reciclado vira negação de serviço barata. Decisão: registrar o evento de segurança na hora, deixar as outras tentativas terminarem, e devolver `ImpressaoNaoBate` só se nenhuma vencer. Custo se errado: o relato da impostura sai alguns milissegundos depois — contra uma DoS, é troca fácil.
Task 4: Ruling (achado Minor 3, promovido): a falta de `fe80::/10` no lado IPv6 de `como_chegou` entra no conserto, e não fica como minor. `ComoChegou` **é** a métrica que o subprojeto A existe para produzir; um link-local contado como `Furo` corrompe o número que decide o desenho do subprojeto B. A parte de desenho do mesmo achado — IPv6 global sem NAT também vira `Furo` — continua diferida para a Task 11 junto com o hairpin. Custo se errado: numerador inflado, e a árvore do B desenhada sobre ele.
Task 4: minor (deferred): asserção `veio == impressao(&ia)` seria mais forte que `len()==64` + `assert_ne!`.
Task 4: minor (deferred): `if let` onde cabia `let … else` na leitura da variante.
Task 4: minor (deferred): o auxiliar de teste `atender_em_segundo_plano` engole falha de aperto de mão; a Task 5 provavelmente o remove.
Task 4: fix round 1/5 despachado ao implementador original.
Task 4: fix round 1/5 (3 addressed, 0 open — rastro por endereço nos três pontos; preempção removida com guarda próprio; `fe80::/10` no lado IPv6; commits a6eee0d..5497c97). Sem quebra nova. O re-revisor verificou o guarda por reversão: 10/10 falhas determinísticas sem o conserto, 30/30 passes com ele, e julgou o atraso de 150 ms do teste como ordenação por construção, não corrida estreitada.
Task 4: complete (commits 861b3b4..5497c97, review clean)
Plano/spec: commit a4e0f66 registra a Task 5 nova e o §3.2.1 da spec.
Task 5: implementador DONE (commit 48067d4; 12 testes em par::, 263 no crate, clippy e check --workspace limpos; quatro reversões provadas). Cresceu além do brief: o brief, implementado literalmente, não fazia os próprios testes passarem — quem discava não apresentava certificado de cliente, e a recusa do lado que atende chega como fechamento assíncrono, não como erro de conexão. Soluções do implementador: uma tabela global por endereço local para achar a identidade de quem disca, e um período de graça pós-conexão com variante `RecusadoDepoisDeLigar`.
Task 5: revisão — spec ❌ (RecusadoDepoisDeLigar(String) viola ADR 0012 e specs/02), qualidade needs fixes: 5 Important, 6 Minor. O núcleo de segurança foi julgado correto e provado correto — o revisor verificou sozinho que `client_auth_mandatory` só morde por causa do teste que o implementador escreveu por conta própria.
Task 5: Ruling: a tabela global sai, e `ligar` ganha `identidade_propria: Option<&Identidade>`. O argumento do implementador (o brief fixa a assinatura de `passar_a_atender`) é verdadeiro e não se aplica a `ligar`, que não tem chamador fora de `par.rs` e cuja irmã `config_de_cliente` ele já mudou. O que decide não é elegância: a tabela dá residência de processo a uma chave privada que o módulo documenta como «nunca vai para o disco», sem revogação, e uma porta reciclada faria alguém se autenticar com uma identidade de empréstimo que ninguém lhe deu — quebra a invariante de consentimento do §5 da spec, não só um handshake. Custo se errado: seis call sites de teste tocados.
Task 5: Ruling: a folga de 2×RTT vira um ida-e-volta de aplicação. O revisor mediu que ela é a **única** linha do lado de quem disca, e o `CONNECTION_CLOSE` do QUIC não se retransmite sozinho — perdido aquele datagrama, `ligar` devolve `Ok` sobre uma conexão que o outro lado já recusou. Não é buraco de confidencialidade (quem recusa continua recusando), é o contrato de `ligar` mentindo, e um estado errado silencioso é o defeito nº 1 desta casa. O custo é menor do que parece: hoje toda discagem já paga piso de 20 ms, e 200 ms numa WAN de 100 ms de RTT. Custo se errado: um ida-e-volta a mais no caminho de ligação.
Task 5: Ruling: `RecusadoDepoisDeLigar` passa a carregar motivo enumerado. O revisor achou o material: `TransportErrorCode::crypto(u8)` na faixa 0x100..0x200 distingue alerta 116 (não apresentou certificado) de 40/42/48 (apresentou o errado) das demais causas. Custo se errado: uma variante a mais no enum.
Task 5: minor (deferred): `# Errors` de `ligar` sem a variante nova; campo de tupla sem doc; dois testes quase idênticos entre T4 e T5 (plan-mandated, fundir); a razão da vida das conexões sumiu com o auxiliar removido; `passar_a_atender` chamada duas vezes troca tudo em silêncio sem dizer no doc.
Task 5: ⚠️ do revisor, resolvido por mim: nada fora de `par.rs` chama `passar_a_atender`/`ligar`/`atender` — a parede está provada em teste e ainda não ligada ao produto. Correto e esperado: quem liga é a Task 8, e o `impressao` do `SirvaTelaPara` só existe a partir da Task 6. Não é lacuna.
Task 5: fix round 1/5 despachado ao implementador original.
Task 5: fix round 1/5 (7 addressed, 1 nova aberta; commits 48067d4..5c5fd8b). O mecanismo novo do achado 5 foi examinado com dureza e aprovado: o prazo velho falhava **aberta** (sleep esgotado ⇒ Ok), o novo falha **fechada** (prazo de quem chama esgotado ⇒ Err), e a troca de byte provoca o CONNECTION_CLOSE que o QUIC não retransmite sozinho (RFC 9000 §10.2.1). Re-revisor conferiu por reversão: 12 verdes → 9 verdes / 3 vermelhos.
Task 5: quebra nova (Important) introduzida pelo conserto: `confirmar_para_quem_ligou` espera `accept_bi()` sem prazo, e `atender` serve uma vaga só — par autenticado e parado nega a vaga por 30 s (ocioso padrão do quinn) ou indefinidamente se mandar tráfego. `ligar` ganhou prazo; `atender` não.
Task 5: Ruling: promovo dois Minor a este round. (a) a classificação de `MotivoDaRecusa` não tem guarda de teste — o re-revisor mediu por sonda que hoje está certa, e uma regressão que jogasse tudo em `Outro` deixaria a suíte verde; é o «existir não é funcionar» literal, e o conserto é um `matches!` em dois testes que já existem. (b) `ligar` devolve `NaoAlcancou` para um par que apertou a mão e travou — «nenhum endereço respondeu» é falso ali, e é a distinção que o próprio `ErroDePar` existe para fazer. Custo se errado: um round a mais numa tarefa que já teve dois.
Task 5: correção factual pedida ao implementador: o relatório diz que a reversão derruba dois testes; derruba três.
Task 5: minor (deferred): `MotivoDaRecusa::Outro(String)` ainda leva texto de erro à interface; `# Errors` de `ligar` sem a terceira variante; `Identidade: Clone` alarga a superfície pública.
Task 5: fix round 2/5 despachado ao implementador original.
Task 5: fix round 2/5 (3 addressed, 0 open — prazo próprio em `atender` com o porquê no doc e o endereço no rastro; `matches!` com a variante exata nos dois testes de parede; `ConfirmacaoNaoChegouATempo` no lugar do `NaoAlcancou` mentiroso; commits 5c5fd8b..c562e81). Sem quebra nova.
Task 5: desacordo de medida arbitrado por reprodução independente — worktree separada no commit do round 1, 15 rodadas: 2 falhas determinísticas, 0/15 da terceira. O implementador estava certo e não arredondou para baixo; a terceira queda que o re-revisor do round 1 viu foi corrida rara. Registrado porque a honestidade dele em contradizer o número que eu passei é o comportamento que se quer.
Task 5: complete (commits a4e0f66..c562e81, review clean, 2 rounds)
Task 6: revisão — spec ✅, qualidade aprovada com 2 Important e 1 Minor. O revisor conferiu a posição das variantes lendo os enums inteiros, provou o guarda ordinal inserindo uma variante intrusa no meio (falhou, como devia), e conferiu na API do GitHub que a v3 saiu mesmo em 12a6401a6 — a premissa da tarefa é fato, não suposição.
Task 6: Ruling: o braço provisório de `state.rs:1213` ganha dono explícito na Task 8, e o comentário dele é corrigido agora. O revisor tem razão: nenhuma das onze tarefas tocava `state.rs`, `Room::apply` é chamado em produção pelo `seele-ffi:3358`, e o comentário citava `crate::caminho` — que é o módulo de **banda de subida**, sem relação com o caminho entre pares. Confundir os dois «caminhos» era garantia de o braço ficar órfão. Custo se errado: uma mensagem v4 descartada no cliente sem ninguém saber.
Task 6: Ruling: os dois braços provisórios sobem de `debug!` para `warn!` enquanto existirem. `debug!` vem desligado em produção, e o precedente do próprio `session.rs:1488` usa `warn!` para coisa menos grave. Um pedido de cliente que é descartado sem resposta é «algo que a pessoa pediu não aconteceu» — o nível reservado a isso nesta casa é `warn`. Custo se errado: duas linhas de log a mais numa branch não lançada.
Task 6: fix round 1/5 despachado.
Task 6: fix round 1/5 interrompido por limite de sessão da API (429) — o agente morreu antes de commitar, árvore limpa, nenhum dos três consertos aplicado. Confirmado por inspeção: state.rs ainda com `debug!` e com o comentário citando `crate::caminho`.
Task 6: fix round 1/5 redespachado com implementador novo (o anterior não sobreviveu). Ledger e task-6-report.md carregam a memória; nada refeito.
Task 6: fix round 1/5 (3 addressed, 0 open — comentário aponta a Task 8, dois braços em `warn!`, doc em português; commits 68ea9e5..dfd6584). Sem quebra nova. Confirmado que as citações legítimas a `crate::caminho` em state.rs:675/680/685 não foram tocadas.
Task 6: nota — o resumo de testes do implementador citava só «66 passed», que é a última linha do `cargo test --workspace`, não o total. Conferi eu mesmo: 1634 testes verdes, zero falhas.
Task 6: complete (commits c562e81..dfd6584, review clean)
Task 7: primeiro implementador travou (watchdog, 600 s sem progresso) sem commitar e sem relatório. Deixou na árvore: `pares.rs` (159 linhas), lib.rs/server.rs/session.rs modificados. Conferido por mim: compila limpo, os quatro testes do brief existem e passam, e o braço-andaime da Task 6 no session.rs já foi substituído.
Task 7: Ruling: aproveitar o trabalho herdado em vez de descartá-lo, **tratando-o como não confiável até os guardas morderem**. Não posso saber se ele seguiu TDD — ninguém viu aqueles testes vermelhos —, e a prova por reversão dá a mesma garantia que o vermelho daria: um guarda que não prende é acusado do mesmo jeito. Descartar 159 linhas que compilam e passam por causa de uma ordem que não dá para auditar seria pagar caro por cerimônia. Custo se errado: um teste que passa sem prender nada escapa — e é exatamente isso que as quatro reversões pedidas cobrem.
Task 7: redespachado com implementador novo, com as quatro reversões e duas conferências extras (Pares::saiu na saída de sessão; endereço público vindo de connection.remote_address()).
Task 7: revisão — spec ✅, qualidade aprovada, 0 Critical, 0 Important. Verificado além do diff: `connection.remote_address()` é mesmo o parâmetro de `run_session`, o braço de saída é o ponto único por onde toda saída passa, e o andaime da Task 6 foi substituído por tratamento real (nada de log-e-descarte sobrevivendo).
Task 7: minor (deferred, plan-mandated): a doc de `pares.rs` diz que a escolha «aponta o primeiro que declarou», mas `HashMap::values()` não tem ordem de inserção — a escolha é arbitrária entre os elegíveis. O texto é meu, veio do brief. Promete determinismo que o código não entrega. Levar à triagem da revisão final.
Task 7: complete (commits dfd6584..4888f37, review clean)
Task 8: implementador DONE (commit 81787f4; 1639 testes no workspace, fmt e clippy limpos; guarda provado por reversão). Adotou o braço órfão de state.rs com decisão escrita («não faz nada, porque o enlace já agiu sobre a mesma mensagem antes de ela chegar à Room»), sem warn! permanente.
Task 8: Ruling (defeito de desenho meu, revisão do pré-voo R3): **quem só assiste nunca declara identidade.** `EmprestarSubida` é o opt-in de emprestar, e o meu R3 mandou `emprestando: false` chegar com `impressao` vazia — misturando «quem eu sou» com «eu empresto». Mas a parede simétrica da Task 5 exige que **os dois** lados tenham certificado e que o servidor conheça as duas impressões: quem atende confere quem chega contra a impressão que o servidor apresentou. Sem identidade de quem pede, a discagem dele é recusada por `client_auth_mandatory`, e a Task 10 baterá nisso.
Task 8: Ruling: a correção é separar os dois conceitos em `Pares::declarou` — a impressão e os endereços são guardados **sempre**, e só `emprestando` decide elegibilidade em `escolher`. `EmprestarSubida { emprestando: false, impressao: <a sua>, locais: [...] }` passa a querer dizer «não empresto, mas esta sou eu». Custo se errado: uma pessoa que nunca quis emprestar tem a impressão efêmera dela guardada no servidor pela duração da sessão — que é o mesmo dado que ela já usa para receber tela de um par, e some com a sessão.
Task 8: fix round 1/5 despachado, cruzando pares.rs (Task 7) e par.rs/enlace.rs (Task 8), porque o conserto é um só.
Task 8: fix round 1/5 (2 addressed, mas o round NÃO fecha — 2 Important novas, 1 guarda faltando, e o funil julgado inofensivo não é; commits 81787f4..32d6ace).
Task 8: Ruling (quebra nova 1): `session.rs:2329` faz `pares.saiu(session.person)` ao receber `ImpressaoNaoBate` — mas `session.person` é **quem relatou**, a vítima, não o impostor. E `ParFalhou` só leva `screen`, então o servidor nem sabe de quem reclamar. Antes do conserto o braço era código morto; o conserto o tornou alcançável. Decisão: o servidor **lembra a própria nomeação** — ele emitiu `SirvaTelaPara`/`AssistaTelaPor` e sabe quem apontou para qual tela —, e `ParFalhou { screen }` resolve contra isso. Sem mudar protocolo. E a chamada a `saiu(session.person)` sai: punir a vítima é o oposto do conserto. Custo se errado: o servidor guarda uma nomeação por transmissão.
Task 8: Ruling (quebra nova 2, e é afastamento da spec): a implementação abriu uma `ponta_de_pares()` própria, com porta efêmera nova. O §3.1 da spec diz o contrário com todas as letras — «não é escuta nova, socket novo nem porta nova» — e dá a razão: aquela porta já tem mapeamento de NAT vivo, e **o endereço público dela é o que o servidor enxerga na conexão de controle**. Com ponta separada, o `publico` que o servidor grava aponta para a porta de controle, onde ninguém atende, e não há como descobrir o público da porta nova sem o ponto de encontro que a spec dispensou. Decisão: voltar à spec e usar a ponta que já fala com o servidor. Custo se errado: o caminho entre pares divide socket com o controle, que é exatamente o que a spec pediu e justificou.
Task 8: Ruling (funil): `declarar_identidade_de_par` sai de dentro de `conectar_por` e passa a acontecer no vencedor, depois da corrida. O re-revisor mostrou que um candidato perdedor sobrescreve a declaração do vencedor e, ao cair, a apaga pela sessão inteira — `Pares` é chaveado por pessoa e `saiu` também. Custo se errado: uma declaração a menos numa corrida rara.
Task 8: Ruling (achado 5): `RecusadoDepoisDeLigar` ganha variante própria em `MotivoDeFalhaDePar`. O re-revisor argumentou o inverso do que se esperava e convenceu: quase nunca é impostura — é «o outro lado não aceitou **a minha** identidade», e implica a declaração de **quem relata**, não a do par apontado. Os dois pedem consertos opostos no servidor. E acrescentar variante num enum de protocolo v4 custa menos agora do que depois de haver tráfego. Custo se errado: uma variante a mais.
Task 8: guarda faltando: a metade «a impressão sobrevive a `emprestando: false`» não é afirmada por teste nenhum — o re-revisor reverteu `declarou` para o comportamento velho e os 357 testes do seele-server passaram.
Task 8: fix round 2/5 despachado.
Task 8: fix round 2 — o implementador aplicou os cinco rulings e morreu antes de commitar (77 min, 313 chamadas, resposta final sem sentido). Conferi eu mesmo linha a linha: os cinco estão no código, com o raciocínio escrito e citando a spec. Acrescentou `if-addrs = "0.15"` ao seele-core com justificativa do ADR 0002; a árvore compila.
Task 8: Ruling: aproveitar de novo, e de novo tratando como não confiável até os guardas morderem — mesmo raciocínio da Task 7. Despachei alguém para provar os cinco por reversão, escrever os guardas que faltarem, conferir que `set_server_config` na ponta de controle não quebra a conexão com o servidor, e commitar. Custo se errado: um conserto sem guarda escapa — e é exatamente contra isso que este despacho é escrito.
CONTROLADOR — erro meu: li a resposta confusa do implementador da Task 8 como morte e despachei um segundo implementador na mesma árvore. O processo proíbe implementadores em paralelo, exatamente por conflito. O primeiro não estava morto: terminou e commitou 51f6a0f. O segundo foi parado 1 minuto depois de começar, antes de tocar em qualquer arquivo; árvore limpa, sem estrago. A lição: uma resposta final estranha não é prova de morte — o estado do git é, e eu tinha como conferir antes de despachar.
Task 8: fix round 2/5 aplicado e commitado (51f6a0f; 1645 testes no workspace, fmt e clippy limpos; as quatro reversões coladas no relatório). Os cinco consertos entregues, mais `Pares::declaracao_de`, `Comando::DeclararIdentidadeDePar` e `MotivoDeFalhaDePar::NaoFuiAceito`.
Task 8: fix round 2/5 (3 addressed provados por reversão do revisor, 1 addressed sem guarda, 1 meio-addressed, 2 quebras novas Important; commits 32d6ace..51f6a0f).
Task 8: Ruling (achado 3, metade que sobrou): `Pares` é chaveado por `PersonId` e `saiu` também, então a saída de um candidato perdedor apaga a declaração do vencedor — mesma janela, causa diferente da que o round 2 fechou. Decisão: a declaração passa a carregar **qual sessão** a fez, e `saiu` só apaga se a sessão bater. Chavear por pessoa foi o erro; uma pessoa pode ter duas conexões vivas durante uma corrida de candidatos. Custo se errado: um campo a mais por declaração.
Task 8: Ruling (achado 1, guarda faltando): reverter o defeito exato deixa os 361 testes verdes, mesma situação que reabriu o achado 4 no round anterior. Decisão: extrair a decisão «dado um ParFalhou, quem desacreditar» para função pura em `pares.rs` e testá-la — é onde o defeito morava, e é testável sem harness de sessão. Custo se errado: o guarda continuaria dependendo de teste de integração que a Task 10 só vai ter.
Task 8: Ruling (quebra nova 1 — privacidade): `locais_de_pares` publica os endereços de rede local de **todo** cliente, inclusive quem manda `emprestando: false`. O §5 da spec nomeia privacidade como a primeira das duas razões do opt-in, e lembra que um servidor não é necessariamente entre amigos (ADR 0021). Decisão: `locais` só é publicado por quem optou por emprestar. Quem só assiste declara a impressão e nada mais — o público que o servidor vê basta para quem empresta discar, e o furo simultâneo cobre o resto. Custo se errado: um par na mesma LAN que só assiste perde o atalho de rede local e cai no endereço público.
Task 8: Ruling (quebra nova 2): o `ServerConfig` nunca é desinstalado — depois do primeiro `SirvaTelaPara` a porta de controle fica armada como listener pelo resto da sessão, com refletor de amplificação até 3x. Decisão: `set_server_config(None)` quando `atender` devolve a única vaga. E o §3.1 — a propriedade de a ponta fazer as duas coisas ao mesmo tempo — precisa de teste com conexão viva, que hoje não existe: é afirmada por leitura da doc do quinn. Custo se errado: superfície de entrada permanente numa porta que antes só discava.
Task 8: fix round 3/5 despachado ao implementador original.
Task 8: fix round 3 — o implementador travou de novo no mesmo ponto (112 min, 412 chamadas, resposta «vou esperar o teste em segundo plano»), com trabalho não commitado em quatro arquivos que compila. Segunda vez com o mesmo modo de falha: ele termina e não consegue fechar.
Task 8: Ruling: escalado para implementador novo em modelo mais capaz, como o processo manda quando um laço não converge. O modo de falha é comportamental, não de capacidade — ele conclui o trabalho e fica esperando algo. O despacho novo proíbe explicitamente segundo plano e espera. Custo se errado: mais um round numa tarefa que já teve três.
CONTROLADOR — segundo erro meu, mesma família: ao escalar a Task 8 para um implementador novo, eu não matei o antigo. Ele voltou a acordar com a mesma frase de espera e ficou vivo junto com o novo — dois implementadores na mesma árvore de novo. Matei ao perceber; o novo estava rodando havia 7 minutos e o antigo não tinha commitado nada.
CONTROLADOR — a lição, e ela é diferente da primeira: uma notificação de «completed» NÃO quer dizer que o agente morreu; ele pode acordar de novo. Quando eu escalo, matar o anterior é parte da escalada, não uma limpeza opcional. As duas vezes não custaram nada por sorte de tempo, e sorte não é processo.
Task 8: fix round 3/5 (5 addressed, 0 open; commits 51f6a0f..86aea6d). Integridade do commit acidental conferida por exame direto do revisor: nenhuma reversão sobreviveu. Os cinco guardas refeitos por ele, todos mordem. Zero Critical, zero Important.
Task 8: complete (commits 4888f37..86aea6d, review clean, 3 rounds)
Task 8: ACHADO SISTÊMICO para a Task 10 e para a revisão final — os itens 2, 3 e 4 têm guarda sobre a peça extraída e **nenhum sobre o ponto de chamada**. O revisor provou os três: devolver o vazamento de privacidade em `enlace.rs:2710`, tirar `parar_de_atender` de `:2911`, e trocar `desacreditar(apontado)` por `desacreditar(session.person)` — cada um deixa a suíte inteira verde. O item 1 é a exceção e mostra o desenho a copiar: lá o conserto veio com mudança de assinatura, e é o **tipo** que impede a regressão. A Task 10 é o harness de integração; ela tem de cobrir os três pontos de chamada.
Task 8: minor (deferred): `stable_id` do quinn é endereço de alocação e a doc só promete estabilidade durante a vida da conexão — em `serve` ele é usado depois de `run_session` retornar. Janela estreitíssima, padrão pré-existente (`Subida::esquecer` faz igual). Vale uma linha de ressalva no doc de `QuemDeclarou::id_da_conexao`.
Task 8: minor (deferred): o lock de `pares` passou a ser tomado em todo `ParFalhou`, inclusive nos três motivos de rotina em que nada é feito.
Task 8: registrado que `locais_a_publicar(true, ..)` não tem caminho de produção hoje — ninguém manda `emprestando: true` —, então **nenhum par publica endereço local**, e o caminho de LAN do §3.1 fica inerte até o opt-in existir na interface. Não é quebra: é consequência correta do gate, e devia estar no doc.
Task 9: revisão — spec ❌, qualidade needs fixes: 1 Critical, 4 Important, 2 Minor.
Task 9: Ruling (Critical): a marca de quadro-chave deixa de ser inferida do primeiro byte do pedaço e passa a sair de um `Enquadramento` que conta bytes, gêmeo do de `seele-server/src/tela.rs`, morando em `seele-core::tela`. O revisor mediu as duas corrupções — pedaço com dois quadros engole a chave inteira; corpo começando em 1 passa por chave e faz quem recebe acusar o par de anunciar 33 MB. E derrubou a justificativa do meu brief: o ADR 0002 proíbe o **daemon** depender do **cliente**, não o contrário, e o `seele-core` já é dono de `TipoDeQuadro`, `CABECALHO_DE_QUADRO_LEN`, `MAX_QUADRO_LEN` e do leitor inteiro em `Recepcao::proximo_quadro`. O cabeçalho do módulo do servidor **já autoriza** o gêmeo por escrito. Passar a marca por fora não basta: `(Vec<u8>, bool)` continua sem conseguir dizer «a chave começa no byte 7». Custo se errado: um Enquadramento a mais no core, quarenta linhas que o próprio repositório já decidiu duplicar uma vez pelo mesmo motivo.
Task 9: Ruling (Important 2): o clone de `Connection` nos testes não é artefato — é `stopped()` faltando na produção. O revisor aplicou a linha e **removeu a muleta dos dois testes**: 21/21 verdes, cinco execuções. O repositório já aprendeu isso em `client.rs:1746-1763`, com comentário. Custo se errado: `repassar` devolve Ok querendo dizer «parei de escrever» enquanto a doc dá a entender «entregue».
Task 9: Ruling (Important 3): falta `set_priority`. Todo outro fluxo de tela do produto rebaixa para -2 (`seele-core/src/tela.rs:1126`, `seele-server/src/tela.rs:939`), e este abre na prioridade padrão **na subida de quem empresta** — uma máquina que está na chamada de voz. É a regra do §3.2 em pessoa.
Task 9: Ruling (Important 4 e 5): `repassar` é a única função de peso em `par.rs` sem rastro nenhum — zero `tracing::` em 50 linhas, contra 13 no resto do arquivo. E `ErroDePar::Escuta` («não deu para pôr esta ponta a atender») usada para falha de escrita no repasse é plan-mandated e não fica.
Task 9: fix round 1/5 despachado.
Task 9: fix round 1 interrompido por limite de sessão da API; o agente deixou `par.rs` e `tela.rs` modificados, compilando, com o `Enquadramento` já esboçado no core. Redespachado para verificar contra os cinco achados, provar por reversão e commitar — mesmo padrão das Tasks 7 e 8.
INTERVALO — o dono do repositório relatou um bug de campo e pediu conserto; a malha ficou parada durante isso. Resultado: branch `bug/ficha-da-conexao-abandonada`, commit 53c27d0. O bug relatado está consertado e provado; a expulsão ficou por consertar depois de três tentativas minhas, e o achado está escrito no commit — ela parecia funcionar porque outro defeito limpava a sujeira por acidente, e nunca encerrou a sessão do lado do servidor.
Task 9: fix round 1/5 (5 addressed, 1 quebra nova Important; commits 499c9ec..8be5ac3). O revisor refez as reversões dos achados 1, 2, 4 e 5 e todas morderam, e alimentou um corte próprio não coberto — cabeçalho de quadro-chave partido entre pedaços — que também mordeu. Comparou os dois `Enquadramento` linha a linha: formato idêntico, sem divergência.
Task 9: quebra nova (Important): o `Enquadramento` do core chegou sem guarda próprio. O revisor quebrou cinco invariantes dele — teto de quadro, quadro vazio, tipo desconhecido, som como porta, e `comeca_aqui` — e a suíte inteira (278 testes) ficou verde nas cinco. O `comeca_aqui` é metade do Critical deste round: sem ele, um cabeçalho partido vira porta de entrada e sai Modo B por outra via. O gêmeo do servidor tem oito testes dedicados; este tem zero.
Task 9: Ruling: os guardas do `Enquadramento` do core entram agora, no mínimo os cinco medidos. Um conserto de Critical cuja metade sobrevive à deleção com a suíte verde não está fechado — é o «existir não é funcionar» aplicado ao próprio conserto. Custo se errado: cinco testes a mais num módulo que já tem 34.
Task 9: Ruling: o `set_priority` fica sem guarda, e a alegação foi conferida contra o fonte do quinn 0.11.11 — `SendStream` não é `Clone`, `Connection` não devolve fluxo por id, e a prioridade é hint local que não viaja no fio. O revisor observou que extrair `abrir_fluxo_de_repasse` daria um guarda de verdade em ~5 linhas; aceito a proporção do implementador, e registro que o caminho existia. Os outros dois sítios do produto também não têm guarda.
Task 9: fix round 2/5 despachado.
Task 9: fix round 2/5 (2 addressed, 0 open; commits 8be5ac3..120485e). O re-revisor refez as cinco reversões, uma a uma, rodando a suíte inteira do crate: todas mordem o teste certo e nenhuma deixa a suíte verde. Comparou os oito testes novos contra o gêmeo do servidor — adaptações fiéis, e o core é estritamente mais específico nos erros. Sem quebra nova.
Task 9: complete (commits 86aea6d..120485e, review clean, 2 rounds)
Task 9: minor (deferred): `set_priority` sem guarda automatizado — limitação real da API do quinn, conferida no fonte por dois revisores. O caminho para prendê-lo existe (extrair `abrir_fluxo_de_repasse`) e foi recusado por proporção.
Task 10: primeiro implementador morto por limite de sessão da API (reset 17:10). Deixou 845 linhas em `crates/seele-conformance/tests/tela_por_um_par.rs` mais cinco arquivos modificados, com 29 erros E0433 — todos caminhos não resolvidos no arquivo de teste. Conferido por mim.
Task 10: redespachado para terminar, com os três pontos de chamada da Task 8 como alvo explícito: `locais_a_publicar`, `parar_de_atender` e `quem_desacreditar`. Este harness é o único lugar do plano que pode cobri-los.
Task 10: implementador DONE (commit 4f63842; 1671 testes no workspace, fmt e clippy limpos). Os 29 erros eram falta de `quinn`/`rustls` como dev-dependencies no seele-conformance.
Task 10: **defeito de produção achado pelo teste de integração** — corrida em `ligar`/`atender` com endereços local e público apontando para o mesmo socket deixa uma conexão «Incoming» órfã, e o `quinn-proto` 0.11.16 entra em pânico dentro de um `Drop`, **abortando o processo** (SIGABRT). Conserto: `recusar_sobras` em `enlace.rs`, provado por reversão (3/3 abortam sem ele, 5/5 limpos com ele).
Task 10: três dos quatro pontos de chamada cobertos por reversão — `locais_a_publicar`, `quem_desacreditar`, `recusar_sobras`, mais a propriedade de o servidor assumir quando o par morre.
Task 10: o quarto — `parar_de_atender` em `servir_um_par` — ficou **sem guarda, e o implementador disse isso em voz alta**. Ele escreveu o teste, mediu os dois lados da reversão, obteve resultado e tempo idênticos, e **removeu o teste** em vez de entregar um guarda que passa com o defeito no lugar. É o comportamento certo, e vale registrar como tal.
Task 10: revisão — spec ❌, qualidade needs fixes: 1 Critical, 5 Important, 5 Minor.
Task 10: CRITICAL — a afirmação central da tarefa não é dupla. O revisor transformou `RepasseDeTela::pedaco` num no-op (zero bytes atravessando o par) e a suíte ficou **3/3 verde, três execuções**. O quadro que o teste dá como vindo do par é o quadro 0, entregue pelo servidor antes do corte e parado na fila de quem assiste — achado em 21 µs, que é pop de fila, não rede. Nenhum teste desta sequência prova que um byte atravessou um par, que é o subprojeto A inteiro.
Task 10: Ruling: o conserto é o que o revisor descreve e que o próprio autor já usa no segundo teste — drenar a fila antes de `assistir()`, guardar o maior `seq`, exigir estritamente maior depois do corte, **e** somar recepção sustentada: N quadros consecutivos (~um segundo de vídeo) com `copias.agora() == 1` o tempo todo. Um cano desligado não entrega um segundo de imagem. Custo se errado: o subprojeto A inteiro fica sem prova de funcionar.
Task 10: Ruling: `recusar_sobras` muda de lugar — vai para dentro de `par::parar_de_atender`, onde nenhum chamador pode esquecê-lo. Hoje mora em `enlace.rs` com um ponto de chamada só, e o subprojeto B reabriria o abort no primeiro chamador novo. Custo se errado: o pânico do quinn volta como SIGABRT em produção.
Task 10: Ruling: a doc de `enlace.rs:3130` afirma que só `refuse` desindexa. É falso — o revisor conferiu na fonte que `ignore` também chama `clean_up_incoming`, e mediu: com `ignore()` a suíte fica verde. `refuse` continua sendo a escolha certa, por avisar o discador na hora; a razão escrita é que está errada, e afirmação confiante não medida é o que o CLAUDE.md desta casa nomeia.
Task 10: Ruling: o guarda abandonado É fechável, e o revisor achou o caminho que o implementador não viu — `par.rs:1811` já usa o discriminador certo: depois de `parar_de_atender`, põe alguém em `atender` e disca. O sinal está do lado de quem atende, não de quem disca. Fecha determinístico em ~300 ms, sem UDP cru.
Task 10: minor (deferred): `ate(..., || true)` que não espera nada; `ContadorDeCopias` assinando o barramento depois de `compartilhar()`; `std::mem::forget(ponta)`; andaime duplicado de `subida_no_arranque.rs`; os 150 ms de `recusar_sobras` somados ao início de todo repasse, não só ao desarme.
Task 10: fix round 1/5 despachado.
Task 10: fix round 1/5 (6 addressed, 0 open; commits 4f63842..4587999). O re-revisor refez o no-op do `RepasseDeTela::pedaco` — 3 execuções, 3 falhas, sempre no teste certo, sempre gastando a paciência inteira de 30 s em vez de um assert rápido. Instrumentou e confirmou que o piso capturado é o quadro 0, exatamente o falso positivo que fundava o Critical, e que os 30 quadros levam 1,23 s ≈ 30 × 33 ms — recepção em tempo real, não pop de fila. Refez também a reversão do achado 4, com sinal nos dois sentidos.
Task 10: complete (commits 120485e..4587999, review clean, 1 round)
Task 10: deferred: `alvo = pelo_par + 1` no segundo teste é piso frágil que hoje só não vaza por margem — o conserto são três linhas com o helper já no arquivo. O re-revisor recomendou agendar, não bloquear.
Task 10: deferred: o rótulo «diff inteiro» do relatório omite 936 linhas (o próprio arquivo de teste e os manifestos); o número de produção está certo.
Task 11: implementador DONE (commit b3c88c9; 1672 testes, 69 suítes). Conferiu os nomes no fonte antes de citar e **corrigiu o meu brief**: o evento de falha é «o par não veio; a tela vem do servidor», com dois campos — `motivo` (o enumerado) e `erro` (o detalhe) —, e eu tinha escrito só `erro`.
Task 11: ACHADO que muda o que eu disse ao dono: **o opt-in de emprestar subida não tem interface em lugar nenhum.** Conferi por grep: zero chamadores de `emprestar_subida` em `apps/` e no `seele-ffi`; o único do repositório é o teste de integração. Ou seja, o roteiro não pode ser executado ponta a ponta hoje sem alguém mexer em código. O implementador transformou isso no primeiro item de conferência do roteiro em vez de mandar alguém clicar num botão inexistente — decisão certa.
Task 11: fora de escopo, registrado por ele: as seções 1–6 de `docs/teste-duas-maquinas.md` ainda citam um binário `connection` que o ADR 0039 removeu. O documento já estava desatualizado antes desta tarefa.
Task 11: revisão — spec ✅, qualidade aprovada, 0 Critical, 1 Important, 3 Minor. O revisor conferiu cada nome de evento e campo contra o fonte e foi além do pedido: checou que o «clique no nome de quem compartilha» existe mesmo em `main.rs:2123` + `ui/palco-imagem.js`.
Task 11: fix round 1/5 (4 addressed; commit 725be01). Verifiquei eu mesmo, sem despachar re-revisão — são quatro mudanças de texto e a revisão final cobre o diff. Registro a escolha: a frase falsa «o primeiro que declarou» sumiu dos dois lugares que descrevem o sistema (`pares.rs` e o roteiro), e sobrevive só no texto do plano, que é registro do que foi pedido e não do que o sistema é.
Task 11: complete (commits 4587999..725be01, review clean, 1 round)
=== As onze tarefas fechadas. Revisão final da branch inteira despachada. ===
Revisão final (opus, 27 commits, `15a0406..725be01`): veredito «pronta para o dono decidir, não pronta para fundir». Relatório inteiro em `revisao-final.md`. Mediu o que afirma: workspace verde, clippy limpo, fmt limpo, `xtask check-deps` verde (ADR 0002 vale). 3 Critical, 6 Important, 5 Minor, mais a triagem dos diferidos.
Revisão final: o que nenhuma revisão por tarefa podia ver — **três estruturas que supõem uma tela, uma sala e um espectador**. C1 (`RepasseDeTela` sem `ScreenId`), C2 (`Pares::escolher` sem sala de voz = vazamento de conteúdo entre salas), C3 (fim limpo do repasse não vira `ParFalhou`, inclusive no caminho de contrapressão cujo doc afirma uma recuperação que não existe).
Revisão final: julgou os três «não consegui» da sequência e considerou **nenhum** otimismo disfarçado. Conferiu por conta própria que `emprestar_subida` não tem chamador de produção.
Ruling: a onda única de consertos leva C2, C3, I1, I2, I3, I5, M1, M2 e os quatro diferidos «antes de fundir». Razão: exceto C2 e C3, são todos de poucas linhas, e consertar C2 agora — com o contexto quente — é mais barato que consertar um vazamento entre salas daqui a semanas, ainda que hoje ele seja inalcançável. Custo se errado: uma onda grande demais para uma re-revisão escopada julgar bem.
Ruling: C1 entra na onda **proporcional, não completo**. O conserto completo é per-tela no fio do par, que é mudança de protocolo e fundação do subprojeto B. Hoje entra o guarda honesto: `RepasseDeTela` passa a recusar a segunda tela em vez de fundir as duas em silêncio. Custo se errado: o B herda a estrutura de uma tela só e paga a mudança lá — que é onde ela cabe.
Ruling: I4 (o IP de quem assiste entregue sem escolha e sem aviso) **não entra na onda**. Não é defeito de código: é meia decisão de privacidade, e a outra metade — o aviso — foi diferida ao subprojeto B pela própria spec. Vai para o dono como decisão, não para um implementador como tarefa. Custo se errado: a malha ganha botão antes de o aviso existir.
Ruling: I6 (vetor de bytes comum aos três leitores de quadro), M3, M4 e M5 ficam agendados, não bloqueiam. Custo se errado: os gêmeos do enquadramento divergem sem teste que acuse.
Onda final: despachada (opus), BASE 725be01, doze itens. Brief em `onda-final-brief.md`.
Onda final: DONE_WITH_CONCERNS, doze commits `725be01..004322a`, um por item. 1688 testes (16 novos), clippy/fmt/check-deps limpos. Prova por reversão registrada nos itens 1–7 e 9.
Onda final: o implementador levantou cinco preocupações por conta própria, três delas contra o próprio trabalho — guardas do item 6 não isoláveis por reversão (mantidos e ditos em voz alta em vez de passados como provados), desvio declarado no item 4 (identidade é só `ScreenId`, porque o dono não existe do lado do cliente), e o item 3 alargando o contador `drops.tela_sem_dono`, que existe para achar discordância. Nenhuma delas foi escondida atrás de um verde.
Onda final: re-revisão escopada despachada (opus), com as cinco preocupações como pergunta explícita e três provas por reversão exigidas.
Re-revisão da onda: doze itens **endereçados**, nada quebrado que funcionava, e as quatro verificações independentes deram certo — a fonte da sala é a viva (`Occupancy`, e `assentar` a atualiza nos dois sentidos ao trocar alguém de sala), o servidor não entra em estado ruim com `ParFalhou` de tela morta, a trava de versão roda antes de qualquer byte, e os 64 hex são conferidos em `decode`, não num construtor. Três provas por reversão refeitas pelo revisor, com a mensagem de falha de cada. Números remedidos: 1688/0/4, clippy 0, fmt 0, check-deps 11 crates.
Re-revisão: **R1, Important, regressão desta onda** — depois do item 3, todo fim limpo vira `ParFalhou`, e o braço de `ParFalhou` religa o cano **sem perguntar se quem relatou ainda quer a tela**. Como `Assistir { quero: false }` não derruba o caminho do par, um `UnwatchScreen` pode ser desfeito depois. A classe já existia no caminho de erro; o item 3 a estendeu ao caminho de rotina, que é o comum.
Re-revisão: das cinco preocupações do implementador, **a 1 estava invertida** — o guarda que ele chamou de redundante é o **único** mecanismo que solta a vaga no `UnwatchScreen`, e não tem teste. Ele foi duro demais consigo num ponto e não o bastante noutro.
Re-revisão: **a 4 confirmada por medida** — o revisor removeu o `ParFalhou{CaiuNoMeio}` e `quando_o_par_morre_...` passou **3/3**, porque o piso é o quadro 0 da fila FIFO. A propriedade de segurança está afirmada e não presa no braço `Err` do cliente.
Re-revisão: **a 5 subestimada** — quando quem assiste sai da sala, o fluxo do par continua entregando a tela daquela sala a quem saiu, e a conexão não fica ociosa, então nada a mata. É da família do C2, não de escrituração.
Ruling: R1, a preocupação 1 e a preocupação 4 entram numa dispensa de fechamento — **as três, e nada mais**. Razão: R1 é regressão que esta onda criou, e uma limpeza não pode deixar um caminho pior do que encontrou; as outras duas são o teste que falta e o teste que mente, ambos com o conserto já escrito pelo revisor. Custo se errado: uma quarta rodada num ciclo que já deveria ter fechado.
Ruling: verifico eu mesmo essa dispensa, refazendo as provas por reversão, em vez de despachar uma quarta revisão. Razão: são três consertos que o revisor já especificou linha a linha, e há um teste de guarda para cada um. Custo se errado: um defeito entra sem olho de fora — mitigado por eu rodar as reversões com as próprias mãos.
Ruling: a preocupação 5 vai para o **portão do subprojeto B**, ao lado do C2 e do I4, não para esta dispensa. Razão: o conserto pede verbo novo no protocolo, que é fundação do B. Custo se errado: o B começa com conteúdo de sala vazando para quem saiu dela.
Ruling: R2 (`drops.tela_sem_dono` contando rotina), F3 (assimetria de quem empresta saindo da sala) e F4 (a regra dos 64 hex duplicada) ficam agendados. Custo se errado: um contador de discordância que não acha discordância.
