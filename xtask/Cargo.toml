[package]
name = "xtask"
description = "Repository automation. Not part of the product; never shipped."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
# `api/*.json`, para o `check-api` do ADR 0044. Mesma versão dos outros quatro
# crates que já o declaram.
serde_json = "1.0.151"
cargo_metadata = "0.23.1"

[lints]
workspace = true
